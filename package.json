{
  "name": "signvision",
  "version": "6.0.0",
  "description": "Real-time ASL recognition — Keras + MediaPipe, fully in-browser",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "extract-weights": "python3 scripts/extract_weights.py"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=16"
  }
}
